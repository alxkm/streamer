## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- What problem does it solve? For a new operator, say why the JDK API is not enough. -->

## Checklist

- [ ] `./gradlew build` passes locally (this includes the coverage gate)
- [ ] New or changed public methods have Javadoc with `@param`, `@return` and `@throws`
- [ ] Tests cover the empty stream, the single element and the boundary cases
- [ ] `CHANGELOG.md` has an entry under `Unreleased`
- [ ] Public API changes are reflected in `README.md`
